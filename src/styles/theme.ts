const colors = {
  primary: "#334155",
  white: "#FFFFFF",
  green: "#10b981",
  yellow: "#F5b014",
  purple: "#7B34DD",
};

const fonts = {
  roboto: `'Roboto', sans-serif`,
};

const theme = {
  colors,
  fonts,
};

export default theme;
